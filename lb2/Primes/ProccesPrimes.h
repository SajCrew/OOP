#ifndef PROCCES_PRIMES_H
#define PROCCES_PRIMES_H

#include <set>
#include <vector>

std::vector<bool>FindPrimeNumbers(const int upperbound);
std::set<int> GeneratePrimeNumbersSet(int array); //оставить только это в заголов файле

#endif